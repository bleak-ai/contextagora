# Context Loader

## Run locally

```bash
cd product
uv sync
uv run start
```

Open http://localhost:8080

## Run with Docker

```bash
cd product/deploy
docker compose up --build
```

Open http://localhost:8080
