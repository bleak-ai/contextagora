# Context Loader

## Run locally

```bash
cd platform
uv sync
uv run start
```

Open http://localhost:8080

## Run with Docker

```bash
cd platform/deploy
docker compose up --build
```

Open http://localhost:8080
