services:
  context-loader:
    build:
      context: ../..
      dockerfile: product/deploy/Dockerfile
    ports:
      - "8080:8080"
    volumes:
      - context-data:/app/src/context

volumes:
  context-data:
